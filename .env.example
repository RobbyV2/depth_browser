# DepthXR Browser - Single Server Configuration
# =============================================================================
# Axum is the main entry point (SERVER_PORT), proxying to Next.js (PORT)
# =============================================================================

# Application Settings
APP_NAME=DepthXR-Browser

# -----------------------------------------------------------------------------
# Rust Server (Main Entry Point)
# -----------------------------------------------------------------------------
# SERVER_PORT: Port where Rust server listens - main entry point
# Users connect here for both /api, /wisp, and frontend
SERVER_PORT=3030
SERVER_HOST=127.0.0.1

# SERVER_PROXY_URL: URL to proxy non-API requests to Next.js dev server
SERVER_PROXY_URL=http://127.0.0.1:3031

# -----------------------------------------------------------------------------
# Next.js Dev Server (Internal)
# -----------------------------------------------------------------------------
# PORT: Port where Next.js dev server listens (internal only)
PORT=3031
HOSTNAME=127.0.0.1

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
RUST_LOG=info

# Base resolution for depth inference (shorter dimension)
NEXT_PUBLIC_DEPTH_INFERENCE_BASE=384
# Target depth FPS (throttle interval = 1000/fps ms)
NEXT_PUBLIC_DEPTH_TARGET_FPS=15
# Depth filter mode for 3D displacement texture
# Options: nearest | linear | linear-mipmap | quantized | contrast | bilateral
NEXT_PUBLIC_DEPTH_FILTER_MODE=bilateral
